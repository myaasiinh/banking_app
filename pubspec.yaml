name: banking_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  fl_chart: ^0.69.0
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  motion_tab_bar_v2: ^0.3.0
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets :
    - asset/icon/
    - asset/image/


